package com.example.clevercat.sharedClasses.constants

import com.example.clevercat.dataClasses.NumberItem

object Constants {
    val LOG_TAG = "xyz"
    val NON_EXISTING_NUMBER  = NumberItem(-1, -1, -1, -1, -1, -1)
}